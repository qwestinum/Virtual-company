# Virtual Enterprise — QWESTINUM

## Projet

Prototype d'entreprise virtuelle où des agents IA jouent les rôles d'une équipe RH. Le donneur d'ordre dialogue avec un Manager RH virtuel qui est à la fois son **point de contact unique (SPOC)** et l'**orchestrateur** de l'équipe d'agents spécialisés (Job Writer, Publisher, CV Analyzer, Scheduler, Rejection Writer) qu'il dispatche et coordonne. Il n'y a pas d'agent Orchestrateur séparé.

Le MVP couvre le département RH avec deux modalités de travail : **campagnes complètes de recrutement** (cycle R1 à R6) et **sollicitations hors campagne** (livrables atomiques). Le donneur d'ordre interagit par deux canaux : **conversation avec le Manager** et **actions directes via l'interface** (toggles, sliders, clics).

**Positionnement QWESTINUM** : Process First — l'IA appliquée à des processus métier réels. Le projet sert à démontrer le concept à des clients en cabinet de conseil. La mimétique avec le fonctionnement d'une vraie équipe RH est le différenciateur principal face à la concurrence (Limova).

## Spécification fonctionnelle de référence

**`docs/specs/entreprise-virtuelle-rh.md`** est la source de vérité fonctionnelle. Tout comportement métier (rôles des agents, rituels, validations, artefacts, actions directes, escalades) y est défini. Claude Code consulte ce document avant toute décision de design fonctionnel.

**Brief de la session courante** : voir `docs/sessions/SESSION_NEXT.md` (réécrit le 21/08/2026 — priorité BLOQUANTE : mettre la production en conformité (migration `migrate.sql` complète, `NEXT_PUBLIC_APP_URL`, `CRON_SECRET`), puis lots 4-5 « extinction Cal.com »). Dernier compte-rendu : `docs/sessions/SESSION_2026-08-20_21_IMAP_UX.md` — **à lire avant tout diagnostic IMAP ou Vercel**. Specs de référence par domaine : `docs/specs/` (`candidatures-sans-suite.md`, `multi-utilisateur.md` en juillet 2026 ; `scheduling-module.md` en août 2026).

## Stack

- **Framework** : Next.js 16 (App Router, TypeScript strict)
- **UI** : Tailwind CSS + shadcn/ui
- **State** : Zustand (store unique pour MVP)
- **AI** : OpenAI API (GPT-4o pour les agents, Whisper pour la transcription voix)
- **Storage hybride (à partir de la Session 5)** : Supabase pour les données opérationnelles (tables, métriques, journal, RLS), Google Drive pour les artefacts visibles client (FDP, annonces, bilans) — voir §5.2 de la spec.
- **Communication temps réel (à partir de la Session 7)** : Supabase Realtime
- **Migration future post-MVP** : n8n + Supabase comme cerveau externe (non implémenté actuellement)
- **Déploiement** : VPS Hostinger (Session 8)

## Décisions architecturales déjà prises

- **Pas de 3D.** La scène 3D a été abandonnée en Session 2 au profit d'une interface 2D Notion/Linear avec cartes d'agents, lignes de flux SVG, panneau détail. Fond sand. Avatars PNG placeholder avec initiales.
- **MVP = Next.js seul.** Pour le MVP actuel, types, store, agents, UI tournent dans Next.js. Pas de n8n, pas de microservice externe, pas d'event bus distribué. Communication agents = appels de fonction côté client + état dans Zustand.
- **Storage différé.** En Session 3, les fonctions d'accès au storage (`searchExistingJobDescriptions`, etc.) existent mais retournent des valeurs vides ou mockées. Implémentation réelle hybride Supabase + Drive en Session 5.
- **Pas d'auth utilisateur dans le MVP.** Le système est mono-utilisateur (le donneur d'ordre = la personne devant l'écran).
- **Gestion de campagne = onglet « Campagnes ».** La gestion de campagne (liste, création, édition, actions de cycle de vie suspendre/arrêter/reprendre) vit dans l'onglet **Campagnes** du `WorkspacePane` (workspace `/rh/recrutement`), hébergée par `src/components/campagnes/CampaignsWorkspace.tsx` ; les composants sont sous `src/components/campagnes/` (`edit/`, `edit/draft/`). Les primitives partagées (`StatusPill`, `AnimatedCounter`, `tokens`) restent sous `src/components/dashboard/`. Le **Dashboard** (`DashboardView`) est désormais une vue résiduelle (KPIs, candidats, activité, agents), en attente d'une refonte décisionnelle. Pas de route `/campagnes` séparée : la navigation reste homogène via les onglets du workspace.

- **Cycle de vie persisté + création hors chat.** La machine d'états du cycle de vie (`src/types/campaign-lifecycle.ts`, phases `fdp → scoring → intake → announcement → publication`, obligatoires vs optionnelles `postponed`) est **persistée** en base (colonne `campaigns.lifecycle jsonb`, nullable) : `rowToCampaign` lit le lifecycle stocké comme `prev` de `reconcileLifecycle` (machine autoritaire, phases obligatoires réconciliées sur leurs artefacts), `campaignToRow` l'écrit, la route `PUT /api/campaigns` l'accepte. Repli legacy (lignes sans colonne) : reconstruction du `postponed` des optionnelles d'une campagne `active`. Modèle fonctionnel : cf. mémoire `project_campaign_lifecycle`. La **création hors chat** (`edit/CampaignCreateSheet.tsx`, sections pliables `CollapsibleSection`) suit deux invariants UI : (1) **« Enregistrer » par section** confirme+replie+ouvre la suivante (ne persiste rien — seul « Créer la campagne » écrit en base) ; (2) le **nom de campagne suit le champ `job_title` de la FDP** (source de vérité unique, `deriveCampaignName`), et un préremplissage par campagne comparable propose **toujours « Repartir à zéro »** (parité chat Manager). Logique extraite en helpers purs testés (`nextOpenSection`, `deriveCampaignName`, `parseListInputRaw`/`normalizeListInput`). Les éditeurs de listes (missions/compétences) conservent le **texte brut à la frappe** (normalisation au blur) pour ne pas déplacer le curseur.

- **Réception des CV par email (poller IMAP).** Pour qu'une candidature reçue par mail soit traitée (`src/lib/imap/poller.ts`, scheduler 30 s au boot via `instrumentation.ts`), **3 conditions cumulatives** : (1) la **boîte est associée à la campagne** (`associateMailbox`, via le flux « email » de la campagne) — sinon skip **silencieux, sans journal** ; (2) la campagne est **`active`** (le chemin email filtre `status==='active'` ; l'upload chat ne l'exige pas) — sinon `imap_match_inactive_campaign` ; (3) **identifiant `CAMP-XXXX` reconnu** (dans le **sujet** — signal fort, nominal — OU, en **repli**, dans le **corps** du mail) + PJ CV. **Fiche de scoring non validée ≠ CV perdu (audit C4)** : le binaire est **stocké** et mis en file `imap_unmatched_cvs` `reason='pending_sheet'` (campagne connue), journal `imap_cv_received` `pendingScoringSheet:true stored:true` ; la file est **drainée automatiquement** dès que la campagne est active + fiche validée (hook `after()` sur PUT `/api/campaigns` et PATCH `/api/campaigns/[id]`, cœur partagé `src/lib/imap/unmatched-replay.ts` — `drainPendingSheetCvs`, séquentiel, idempotent par réservation conditionnelle). Un échec de stockage/insert dans ce branchement LÈVE (classé re-tentable → curseur gelé), on ne consomme jamais un CV sans état final rejouable. **Invariant rejeu** : jamais de rejeu vers une campagne sans fiche validée (la route replay répond 409 `scoring_sheet_not_validated` — sinon l'upsert `(mailbox, uid, file_name)` no-operait sur la ligne consommée et le CV disparaîtrait). **Rapprochement campagne** (`src/lib/imap/campaign-match.ts`, pur/testé — `resolveCampaignMatch`) : priorité **sujet > corps** ; le corps **refuse de deviner** si ≥2 campagnes ACTIVES distinctes y figurent (`imap_ambiguous_body_match`, le DRH tranche) — un mauvais rattachement silencieux est pire qu'un non-rattachement ; priorité **active > inactive** ; corps = `parsed.text` prioritaire, repli HTML dé-balisé (`emailBodyText`) ; `matchSource` (`subject`|`body`) tracé dans `imap_cv_received`. **Formats CV acceptés** (`src/lib/imap/cv-attachment.ts`, pur/testé) : **PDF + DOCX** (extractibles par `extractCVText`/mammoth), détection **MIME OU extension** (les clients envoient souvent un .docx en `application/octet-stream`). Un **.doc** ancien (non extractible) n'est PAS silencieux : trace dédiée **`imap_cv_unsupported_format`** (jamais noyé dans `imap_email_no_cv`). **Trou `none` corrigé (audit C11)** : un mail dont AUCUNE campagne n'est reconnue mais qui porte un **CV en PJ** est désormais **tracé** (`imap_no_campaign_match`) et son binaire **stocké** (`unmatched/…` en Storage + table `imap_unmatched_cvs`, une ligne par PJ, migration manuelle) — **rejouable** via `POST /api/imap/unmatched/[id]/replay {campaignId?}` (l'HUMAIN choisit la campagne — optionnel pour une ligne `pending_sheet` dont la campagne d'origine est connue ; réservation conditionnelle `pending→replayed` AVANT le traitement, rejeu = `processEmailAttachment` tel quel via le cœur partagé `unmatched-replay.ts`, mêmes gardes et claims — jamais de double mail). Un mail sans PJ CV reste skippé sans bruit. **Un mail = UNE candidature (incident Malaka 30/07/2026)** : l'id d'analyse `can_imap_<mailbox>_<uid>` est unique par MAIL (insert-only, jamais d'écrasement) — analyser chaque PJ séparément faisait gagner la ligne à la PREMIÈRE traitée (la lettre APEC `candidature.pdf`, classée `isCv:false` → « Candidat anonyme » score 0), l'analyse du vrai CV partant en `already_exists`. Sélection désormais : PJ triées par vraisemblance de nom (`orderCvAttachmentsByPriority`, pur/testé — lettres/motivation/`PROFIL` pénalisées, JAMAIS exclues ; tri stable) puis analysées jusqu'à la première dont `analyzeCVApplication` rend `isCv:true` (flag exposé dans la sortie) qui porte LA candidature ; PJ non-CV skippée TRACÉE (`imap_attachment_skipped_non_cv`, rien persisté) ; PJ restantes après le gagnant tracées (`imap_attachments_not_analyzed`) ; DERNIÈRE PJ = dernier recours (voie « Candidat anonyme » historique — jamais une perte muette) ; défaut permanent/abandon d'une PJ ⇒ la sélection CONTINUE sur la suivante, échec re-tentable ⇒ stop (le mail entier repasse par les rails). Le **drain `pending_sheet` groupe par (mailbox, uid)** et applique la même sélection (lignes sœurs consommées tracées `imap_unmatched_sibling_skipped`) ; le rejeu HUMAIN d'une ligne choisie ne skippe jamais (le choix humain prime). `GET /api/imap/unmatched` liste les `pending` ; compteur `unmatchedPending` dans `/api/imap/status`. UI de rejeu = backlog. Diagnostic : `GET /api/imap/status`, `GET /api/imap/debug/<mailboxId>?force=1`, `POST /api/imap/poll-now`. Cf. mémoire `project_email_intake_requirements`.

- **Rails de réessai d'analyse IMAP + « jamais décider sous incertitude LLM » (correctif audit C2/C3).** Un échec LLM n'est JAMAIS une décision : si la phase **verdicts** échoue après retries, `analyzeCVApplication` lève **`AnalysisUnavailableError`** (plus de fallback `non_verifiable` → l'ancien comportement produisait un score ≈ 0 ⇒ **mail de refus auto envoyé à tort**) ; la **narration** (cosmétique) absorbe TOUTES ses erreurs (fallback déterministe — un CV bien scoré ne se perd pas pour un texte raté). Côté poller (`src/lib/imap/poll-retry.ts`) : **un seul rail** `RetryablePollError` (le différé HITL `RetryableOutreachError` en hérite, et **compte dans le plafond**) + classification pure `classifyProcessingError` — **permanent** = défaut PROUVÉ du document (`CVExtractError` hors `pdf_engine_unavailable` — classification CONSERVATRICE : au moindre doute sur l'origine ⇒ transitoire) ⇒ **même état final que l'épuisement** : binaire sauvegardé + `imap_cv_failed` « traitement manuel requis », le curseur avance. **⚠️ Ne JAMAIS ré-envelopper une erreur d'extraction** (`new Error('extract_failed: …')`) : le wrap détruit le type `CVExtractError` avant classification — cause de l'incident 07/2026 (docx illisible parti en retry, file gelée). **Tout le reste** (transport LLM/429/timeout, hoquet DB, verdicts KO) = **re-tentable** ⇒ **DÉCOUPLAGE curseur/retry (incident 07/2026)** : le curseur AVANCE, l'uid attend dans `imap_cv_retries` (compteur durable, migration manuelle) et est **re-fetché NOMMÉMENT** à son échéance (`buildFetchSet` : set UID `"1624,1701:*"` = retries échus + plage des nouveaux ; garde `shouldProcessUid`) — **un CV en échec ne bloque JAMAIS les CV suivants** ; sûr car les claims deux-phases couvrent le re-traitement (jamais de double mail) et le curseur ne rembobinant plus, les uids suivants ne sont jamais re-présentés. **Poll en 2 phases** (collecte IMAP brève → traitement HORS connexion — un socket gardé ouvert pendant les minutes d'analyse LLM mourait et le crash avalé sautait le commit : re-analyses en boucle, incident 24/07) + **commit du curseur PAR MESSAGE résolu** (`nextCommitTarget` pur — un kill Vercel à `maxDuration` n'efface que le message en cours, jamais le lot ; le curseur ne recule jamais). Backoff **1/5/15 min** (fenêtre = uid non fetché, zéro coût LLM), **plafond 3 tentatives (~21 min)** puis **abandon SIGNALÉ** : journal `imap_cv_analysis_abandoned` + binaire sauvegardé en artefact (`art_imap_cvabandon_*`) pour traitement manuel — jamais de refus auto. **Fail-safe INVERSÉ** : réessai non mémorisable (table absente) ⇒ abandon signalé immédiat, jamais un gel sans fin ; le seul frein de boucle restant (`minRetryUid`) est « état final inécrivable » (panne DB — qui empêche de toute façon le commit du curseur). Chat : `/api/cv-analyzer` → **503 `analysis_unavailable`** explicite, rien n'est persisté. Provider : `maxRetries: 4` (SDK, backoff + `Retry-After`) sur OpenAI/Anthropic.

- **Idempotence de l'outreach IMAP (double mail/brief).** Sur Vercel, chaque hit du cron `/api/cron/imap-poll` est une **instance isolée** : la garde anti-réentrance du poller (`__imapPollInFlight__`, en mémoire de process) **ne sérialise pas** deux invocations concurrentes → elles lisent le même `last_uid_seen` (committé en fin de poll seulement) et envoyaient DEUX fois le mail candidat + le brief. La **seule chose partagée entre instances = la base**. Verrou durable `imap_outreach_claims (mailbox_id, uid, mode)` (**migration manuelle + reload cache PostgREST**, sinon `claimOutreach` fail-open et le doublon persiste), sur le modèle de l'idempotence webhook Cal.com : `claimOutreach` réserve **juste avant `sendEmail`** (fenêtre ~ms) → perdu ⇒ `SendResult{kind:'duplicate'}`, on n'envoie rien ni le brief ; `releaseOutreachClaim` si l'envoi n'aboutit pas (anti-perte silencieuse). Cf. mémoire `project_outreach_idempotency`.

- **Zéro troncature silencieuse : pagination keyset + chunking RPC (audit C8/C9/C10).** Principe : aucune liste/comptage/rapport n'est silencieusement tronqué (PostgREST plafonne à **1000** toute requête sans `.range()`, RPC set-returning comprises). Helper unique `src/lib/db/paginate.ts` : `fetchAllKeyset` (curseur = **PK unique+stable**, jamais un timestamp ⇒ zéro trou/doublon aux frontières même sous insertion concurrente ; index range-scan O(log n)/page, pas offset) + `chunk`. Appliqué : listes maîtres vivier (`listIndexedVivierTitles`/`Entities`, `listVivierCandidateIds` = reindex, `listContactedEmailsSince`/`listRejectedEmailsForCampaign` = cooldown, `listPreselection`), exclusion « déjà postulé » (`loadExcludedEmails` → `listAllCandidateAnalyses` exhaustif — sinon défaut 200 → réinvitations), rapports clôturés (`assembleCampaignReport`/`loadClosedCampaignReports` → `listAllCandidateAnalyses` scopé aux ids clôturés ; `journey-lookup` → `listJournalEntriesByActions` sans cap 500 pour les marqueurs entretien/recruté). **RPC vectorielles** (`matchVivierAnchors` 3 lignes/candidat, `listSkillEmbeddingsByCandidateIds` N skills/candidat) : **chunk d'ids (300/500)** pour garder chaque retour sous le cap — garantie DURE indépendante du volume (le filtre SQL `min_similarity` reste un perf follow-up : ne PAS ajouter de param RPC sans migration-avant-déploiement, risque de skew). **Limites légitimes** conservées (fenêtres « récents » du journal 30/50/500, `searchFdps` top-5, `.limit(1)`, paginations UI). Test à volume : `npm run seed:volume` (1300 vivier + 300 analyses + 600 journal, vecteurs seedés, zéro OpenAI, garde ref projet). Cf. mémoire `project_silent_truncation_c8`.

- **Idempotence des envois HITL + claims deux-phases (audit C5/C6/I7).** Fil rouge : un envoi est **exactly-once** et le journal dit la vérité. (1) **Claims deux-phases** (`imap_outreach_claims`, `calcom_webhook_events` — colonne `confirmed_at`, migration manuelle) : posé AVANT l'effet, **confirmé APRÈS** ; un conflit rend un verdict précis — `already_sent`/`already_delivered` (prouvé, final), `in_flight` (jeune non confirmé → **différer** : IMAP gèle le curseur, Cal.com reçoit 500 et re-essaie), reprise conditionnelle d'un claim **périmé non confirmé** (orphelin de crash, TTL **5 min** partagé — `src/lib/db/claims-policy.ts`, pur/testé). Release garanti sur exception (catch). Fenêtre résiduelle assumée : crash entre envoi et confirmation ⇒ rare doublon (« mieux qu'un candidat muet »). (2) **Machine d'états HITL** `pending → sending → sent` (`pending_validations`, CHECK migré + `sending_at`) : réservation serveur `POST /api/validations/[id]/reserve-send` AVANT tout envoi (un seul gagnant, 409 sinon ; `sending` périmé re-réservable après TTL — jamais un piège) ; **décision immuable dès la réservation** (PATCH decision conditionné `status='pending'` → 409 `decision_locked`) — « invitation + refus » impossible par construction ; pour TOUS les lecteurs (`listPendingValidations` inclut `sending`), un `sending` est « encore en attente ». (3) **Claim humain** : `mail-composer` avec `validationId` pose le claim `('hitl_validation', validationId, mode)` dans la **même table** — un retry ne renvoie jamais (`status:'duplicate'`, traité comme succès par le client). (4) **Journal honnête** : `hitl_validation_sent` porte `mailStatus`/`mailSent` réels + action dédiée **`hitl_mail_not_sent`** quand le mail n'est pas parti (décidés-jamais-contactés requêtables) ; `imap_outreach_duplicate_skipped` ne s'écrit que sur claim **confirmé**. Cf. mémoire `project_hitl_send_idempotency`.

- **Pré-remplissage d'une campagne à partir d'un document (appel d'offres / notes).** Chemin **formulaire uniquement** (le volet conversationnel — `chooseRouteBrief`, chips de pondération — a été **abandonné** avec la refonte Manager lecture seule) : on LIT un document (distinct du Job Writer qui GÉNÈRE), on en extrait un **brouillon**, l'humain valide. **Aucune persistance avant validation.** Extraction texte PDF/**DOCX** via `extractCVText` (mammoth ajouté, server-only, partagé avec l'analyse CV) → extracteur LLM `src/lib/agents/campaign-prefill.ts` (`extractCampaignPrefill`, via `chatCompleteJson`, provider abstrait) → route commune `POST /api/campaigns/prefill` (FormData `document`). Schéma + mappings **purs** dans `src/types/campaign-prefill.ts` (`CampaignPrefill`, `prefillToFDP`, `prefillToSuggestedCriteria`, normalisation séniorité/contrat). **Le LLM n'invente jamais** (champ absent → `null`), retient la **dernière mention** sur contradiction (`conflit`). **Deux catégories** : factuels (`suggere:false`) et **pondérations suggérées** (`suggere:true`) ; les **seuils & flags éliminatoires sont hors périmètre absolu** (saisie 100% humaine — `SUGGESTABLE_LEVELS` exclut `redhibitoire`/`obligatoire`). **Garde-fou commun** : `ScoringCriterion.suggere` + `countUntreatedSuggestions` ; `campaigns-store.activateCampaign` **refuse le lancement** tant qu'une pondération suggérée n'est pas **traitée** (confirmer→`suggere:false` / rejeter→retrait). UI (`CampaignCreateSheet`, `ScoringDraftEditor`) : badge « Suggéré par l'IA », confirmer/rejeter + **tout confirmer/tout rejeter**, `SourceHint` au survol, `CreatedStep` bloque l'activation (« N pondérations à traiter »). Traçabilité : extraction captée dans `campaigns.prefill_extraction jsonb` (nullable, **migration à appliquer manuellement**).

- **Manager RH = agent LECTURE SEULE (consultation + orientation).** Refonte majeure : le Manager conversationnel ne MUTE plus jamais l'état (création/édition/suppression/activation de campagnes, statuts, pondérations, présélections) — **tout ce qui écrit passe exclusivement par l'UI déterministe**. Il SAIT, ANALYSE, ORIENTE. **Trois capacités autorisées** : (1) analyser un CV déposé contre une campagne ; (2) faire le point sur une campagne à partir de **données LIVE fournies par le code** (le LLM narre, n'invente aucun chiffre, dit si une donnée manque) ; (3) orienter dans l'UI. **Architecture du tour** (`src/lib/agents/manager.ts`) : **classifieur déterministe** (`buildIntentClassificationPrompt` — routage + injection des données) → **tour LLM de FORMULATION** (`buildManagerReadOnlyPrompt`, gpt-4o) cantonné à la rédaction d'une réponse de lecture/orientation **chaleureuse, tutoiement, pédagogue**. **VERROU code** : `runManagerTurn` supprime `fieldExtractions`/`proposalField` de la sortie LLM — jamais d'écriture, même si le LLM dérape. **Anti-hallucination navigation** : les chemins viennent EXCLUSIVEMENT de la **cartographie produit** (`src/lib/agents/manager-cartography.ts` — libellés EXACTS de l'UI, à tenir à jour, injectée en contexte) ; chemin absent → le Manager **avoue son incertitude** plutôt que d'inventer un menu. **Reconnaissance de nature d'un document déposé** AVANT toute analyse/comptabilisation (`src/lib/agents/document-nature.ts` + `POST /api/documents/classify`, `ManagerChat.handleFilesSelected`) : CV → analysé ; **appel d'offres → orienté vers Campagnes, JAMAIS analysé ni compté** ; autre/illisible → averti (`documentNatureNotice`). Refonte menée par phases : neutralisation des écritures (Phase 2), nettoyage partiel du cadrage write mort (Phase 3 lots 1-2) ; le reste du code mort (handlers `ManagerChat`, stores `fdp-store`/`scoring-store`, composants de cadrage) est **consigné au backlog** (Phase 3 lots 3-4, `docs/BACKLOG.md`). Cf. mémoire `project_manager_read_only`.

- **HITL = 3 zones de décision PAR CAMPAGNE (deux seuils).** Remplace le seuil unique + le HITL global. Chaque campagne porte `threshold_low ≤ threshold_high` : `< bas` refus auto · `[bas, haut[` **zone grise** (validation humaine via `pending_validations`) · `≥ haut` acceptation auto. `scoreCandidat` (`src/lib/scoring/score-candidat.ts`) calcule le score puis `classifyDecisionZone` → `ScoreResult.decisionZone` (la **vérité** ; le `status` binaire reste `rejected` provisoire pour un gris). Gate `src/lib/hitl/outreach-gate.ts` piloté **par la zone** (chat ET IMAP, zéro duplication) ; décision humaine d'un gris → `updateCandidateAnalysisDecision` (`decided_by='user'` + identité, zone immuable). **Persister `scoringResult.decisionZone`** en base (PIÈGE : ne PAS re-dériver du statut — sinon jamais de `gray`). Reporting (`aggregations.ts` `computeVolumes`) : volumes 3 zones (retenu / écarté / **en attente** / décidé système / tranché humain) + « validation humaine » (= part en zone grise) remplace « arbitrage ». Parcours d'audit (`candidate-journey.ts`) gated par `decisionZone`, `humanIntervention = decidedBy==='user'`. **Retirés** : colonne `campaigns.threshold`, HITL global `app_settings.hitl_config` (snapshot `candidate_analyses.hitl_config` conservé), niveau de scoring `obligatoire`/HARD_CAP (dormant). Doc complète **fonctionnelle + technique : `docs/specs/hitl-3-zones.md`**. Cf. mémoire `project_hitl_three_zones`.

- **AUCUN refus n'est envoyé automatiquement (conformité RGPD, 18/08/2026).** Renversement de la règle d'origine : la zone sous le seuil bas **n'envoie plus**, elle met en file et **PROPOSE** un refus. Zones vivantes : **`proposed_reject`** (`< low` — file HITL, aucun mail, sous-onglet « Propositions de refus ») · `gray` (`[low, high[` — file HITL, « À examiner ») · `auto_accept` (`≥ high` — **seule** zone qui envoie encore seule : décision FAVORABLE et réversible). **`auto_reject` est LEGACY** : plus jamais produite, CONSERVÉE — elle marque les refus réellement partis sans validation avant la bascule ; la fondre dans `proposed_reject` ferait basculer rétroactivement tout l'historique en « en attente » et changerait les chiffres de rapports déjà envoyés. **Le « seuil de proposition de refus » EST `threshold_low`** — pas de quatrième réglage (une colonne `rejection_proposal_threshold` a existé quelques heures : elle bornait une sous-file À L'INTÉRIEUR du gris et ne supprimait aucun envoi ⇒ contresens, supprimée). **Point d'application UNIQUE `gateCandidateOutreach`** : il liste la zone qui ENVOIE et met tout le reste en file — jamais l'inverse, pour qu'une zone ajoutée demain attende par défaut ; les replis des appelants (chat, IMAP, `deriveDecisionZone`) retombent sur `proposed_reject`, JAMAIS sur `auto_reject`. **Lecteurs sensibles** (prédicat partagé `isAwaitingHumanZone`, ne pas dupliquer) : `deriveCandidateStage` (`decidedBy==='user'` prime ⇒ « Non retenu » quelle que soit la zone ; zone en attente sans ligne de file ⇒ « À valider », jamais un refus consommé), `computeVolumes` (`enAttente` = gris + proposés ; `decidedBySystem` lu sur `decidedBy`, plus sur la zone), `deriveJourneyFor` (un gris passe pour RETENU au screening — son score est dans la bande ; un `proposed_reject` a bien échoué, mais son refus est PROVISOIRE via `rejectionGated`), `zoneDistribution` (déjà agnostique). Migration : CHECK `candidate_analyses_decision_zone_chk` étendu (bloc CANONIQUE unique, drop+add).

- **Refus groupé = un GESTE, jamais un nouveau chemin de décision.** Sous-onglet « Propositions de refus » de Validation suspendue, borné par `threshold_low`. **Partition stricte sur la ZONE FIGÉE AU SCORING** (`src/lib/hitl/rejection-proposal.ts`, pur/testé — `partitionRejectionProposals` lit `decision_zone`, JAMAIS une comparaison du score au seuil bas COURANT : les seuils bougent, la zone d'un dossier déjà analysé non — défaut observé en recette, un gris à 80 basculait en proposition après passage du seuil bas à 93, re-jugé avec un barème jamais connu), servie par `GET /api/validations` (`zoneByValidation`, rapprochement par `payload.uid`, chunké) : l'onglet « À examiner » et `ValidationCard` sont **intouchés**, la carte est réutilisée telle quelle sous une couche de sélection (accepter depuis le sous-onglet suit donc le chemin d'acceptation normal). Tri **score DÉCROISSANT** (les cas limites en tête), signal au-delà de ~200. **Exécution** (`bulk-reject.ts`) : **séquentielle**, une candidature à la fois par `decideGrayValidation` → réservation + claim + finalisation propres à chacune ; un échec **n'arrête pas** la fournée et laisse la candidature `pending` (visible, retentable) ; brouillon indisponible ⇒ on ne décide PAS (jamais de mail vide, jamais de candidature « traitée » sans envoi). Confirmation **obligatoire** (liste nominative + score). Case **« envoyer les mails »** cochée par défaut ; décochée ⇒ `mailStatus='skipped_by_user'` et le journal **DISTINGUE le choix de la panne** (`hitl_mail_not_sent.payload.cause`), sous une action unique pour garder les décidés-non-contactés requêtables ; `batchId` journalisé des deux côtés. **Zéro troncature** : `listPendingValidations`/`listVoidValidations` passent en keyset (`fetchAllKeyset`, curseur PK). Régression **S15**. Doc : `docs/specs/hitl-3-zones.md` §4.1-4.2.

- **« Classée sans suite » = terminal ORTHOGONAL au refus (jamais un 3ᵉ statut).** Fin de vie propre des candidatures non traitées (campagne clôturée, poste pourvu, candidat retiré, sans réponse, doublon, invalide — enum `src/types/dismissal.ts`). Modèle : colonnes `candidate_analyses.dismissed_at/dismissal_reason/dismissed_by(+user_*)` **nullable** — `status`/`decision_zone`/`decided_by` restent INTACTS (un 3ᵉ statut contaminerait `deriveDecisionZone`, l'audit et le PDF en « Écarté »). Classement = update **conditionnel** `.is('dismissed_at', null)` ; garde symétrique sur `updateCandidateAnalysisDecision`. Satellites fermés : `pending_validations` **`void`** (transition UNIQUEMENT `pending→void` — un `sending` DIFFÈRE le classement, jamais classer sous incertitude), `interview_briefs` **`cancelled`** (booking Cal.com posthume bloqué ; réouverture restaure via `booking_uid`). **8ᵉ stage `sans_suite`** (prioritaire sur TOUT, ton neutre) : les signaux métier s'éteignent **par construction**. Compteurs : catégorie PROPRE partout (ruban, Bureau 5 catégories — total complet, volumes `classeeSansSuite` — la partition somme) ; **taux des rapports sur les ÉVALUÉES** (reçues − sans suite, note de bas de page PDF). Cœur partagé `src/lib/candidatures/dismissal.ts` (`dismissCandidature`/`reopenCandidature`) + batch `dismissal-batch.ts` (énumération via `stageFor` CANONIQUE) ; mail d'information = template pur `dismissal-template.ts` (matrice par raison — JAMAIS doublon/invalide ; ton sans-refus, mention RGPD) sous **claim deux-phases** `('candidature_dismissal', analysisId, 'dismiss')` — rejeu ⇒ zéro second mail. Flux : dialog partagé `CampaignDismissFlowDialog` sur les 3 clôtures (carte, Sheet, sélecteur chat — plus de `window.confirm`) + flux GO non bloquant (`poste_pourvu`) + action individuelle (raisons individuelles seulement, 400 sinon). **La clôture passe par `POST /api/campaigns/[id]/close`** (seul chemin qui pose `closed_at` — le PUT snapshot ne le fait pas). Réversible : « Rouvrir » (`revert` conditionnel + `void→pending` + restauration briefs), trace conservée au journal (`candidature_dismissed`, `candidature_dismissal_mail_not_sent`, `candidature_dismissal_reverted`, `campaign_closure_dismissals`). Régression : S9 + invariant de partition S6 étendu. ⚠️ Deux pièges NON compilables couverts par tests : `CANDIDATE_STAGE_RIBBON_ORDER` (tableau) et `ZoneDistribution` (littéral 4→5 zones).

- **Multi-utilisateur : espace COMMUN, agendas et admin individuels.** Plusieurs recruteurs partagent tout le métier (campagnes, candidatures, vivier, compteurs — **aucun cloisonnement de données, choix assumé**). Individuel : l'agenda Cal.com, l'identité dans les actions (pattern `decided_by` existant, capturée serveur), l'accès admin. **Référentiel `recruiters`** (id = auth.users.id sans FK, `calcom_link`, `role admin|member`, `is_active` — désactivation douce, jamais de suppression ; RLS activée sans policy, seed admin par email). **Référent de campagne** `campaigns.owner_user_id` (nullable, backfill admin) — bloc « Recruteur référent » dans l'édition. **Résolution d'agenda = POINT UNIQUE** `getResolvedAgendaLink(campaignId)` / `buildInterviewMail` (`interview-mail.ts`) : référent actif avec lien perso → `interviewConfig.agendaLink` global → env `CAL_COM_EVENT_URL` → gate « invitation bloquée » ; fail-soft à chaque étage. ⚠️ Le **preview HITL est le point de vérité du lien** (l'override envoie le HTML relu tel quel — toute logique de lien s'applique au preview, pas à l'envoi). **Cal.com option A** : comptes séparés par recruteur, **un webhook PAR compte** (même URL + MÊME `CAL_COM_WEBHOOK_SECRET` — la route n'en vérifie qu'un) ; piège documenté : un webhook *team* ne couvre PAS les event-types personnels des membres ; le matching booking→candidat reste par EMAIL candidat (indifférent à la source) ; `organizer.email`/`eventTypeId` captés au webhook (traçabilité journal, jamais matching). **Autorisation applicative** (le service_role bypasse la RLS) : rôle lu de `recruiters.role` SEUL (pas de custom claim) — proxy gate `/admin` (lookup limité au préfixe, fail-closed) + `requireAdminApiUser()` (cache 60 s) sur les routes techniques (`/api/metrics/campaigns/*`, `/api/imap/*`, `/api/email/status`, `/api/artifacts/test-connection`, `/api/recruiters`) ; `/api/metrics/global` **SCINDÉ** (member : `agents: []` + `costEstimate: 0`, le récit Bureau reste servi) ; `/api/recruiters/options` = projection minimale pour le sélecteur (toute session). **CRON_SECRET fail-closed** (I13 : absent ⇒ 500, `timingSafeEqual` — ⚠️ poser la variable AVANT de déployer, runbook `docs/ops/multi-utilisateur.md` §0) ; `?next` du login assaini (`sanitizeNextPath`, anti open-redirect). Régression S10 ; procédure onboarding recruteur + webhook : `docs/ops/multi-utilisateur.md`.

- **Module de réservation NATIF (`src/lib/scheduling/`) — autonome, destiné à remplacer Cal.com.** Spec de référence : **`docs/specs/scheduling-module.md`** (lots 1 à 5). **Lots 1-2-3 livrés** (cœur + surfaces candidat + intégration ORQA), **lots 4-5 à faire** (extinction du stock Cal.com, décommission). **COEXISTENCE : un flag PAR CAMPAGNE** (`campaigns.scheduling_native`, défaut `false`) au point de résolution unique — OFF ⇒ chaîne Cal.com **inchangée** (webhook, matching par email, `recruiters.calcom_link`, `interviewConfig.agendaLink` : rien n'a bougé, S10.1-S10.3 le prouvent) ; ON ⇒ lien nominatif à usage unique sur les disponibilités du référent.
  - **FRONTIÈRE D'AUTONOMIE — non négociable.** `src/lib/scheduling/**` n'importe **RIEN** en `@/` : ni type métier, ni repo, ni composant, ni jeton de design. Le module ignore candidat, campagne, brief, recruteur ; il ne connaît que des **ressources** réservables, des **cibles** re-pointables, des **liens** nominatifs, des **réservations** et des **événements**. Les clés (`external_ref`) et charges utiles (`context`, `display`) de l'hôte sont **opaques** : stockées et restituées telles quelles. Communication par **ports injectés** (`configureScheduling({ supabase, mailer, publicBaseUrl, labels })`) et par événements. Gardée DEUX FOIS : règle ESLint `no-restricted-imports` (`eslint.config.mjs`) **et** `src/lib/scheduling/__tests__/frontier.test.ts` (imports hôte, dépendances externes non déclarées — seules `luxon`, `@supabase/supabase-js`, `react` sont admises — et **vocabulaire métier jusque dans les commentaires**). Les deux ont été sondées : elles échouent bien sur une violation. Le seul point de contact est l'adaptateur `src/lib/scheduling-host/` — **hors** du module, délibérément.
  - **La « cible » (`sched_targets`) est le geste central.** Un lien pointe une cible re-pointable, JAMAIS une ressource : changer de titulaire bascule tous les liens déjà émis sans réémission, tandis que les RDV déjà pris **figent** leur ressource et ne bougent pas. `version` incrémentée à chaque re-pointage = contrôle optimiste dans la séquence de confirmation.
  - **SURFACES PUBLIQUES** : pages `/r/{token}` (réserver) et `/b/{manageToken}` (déplacer/annuler), routes `/api/sched/**`. **Authentification = le JETON PORTEUR de l'URL** (128 bits base64url, usage unique, expirable, révocable) — il n'y a pas de session, et c'est voulu : l'invité n'a pas de compte. Écrans dans `src/lib/scheduling/ui/` (CSS brut + variables `--sched-*`, **aucun** composant ni jeton ORQA), point d'entrée séparé `ui/index.ts` pour qu'une route serveur n'embarque pas React.
  - **URL PUBLIQUE = `NEXT_PUBLIC_APP_URL`, à POSER explicitement en production.** Elle sert DEUX fois : le lien nominatif envoyé au candidat, et la moitié droite de l'UID iCalendar (`UID:<racine>@<hôte>`, `icsDomain()`). Le module prend grand soin de rendre la moitié GAUCHE stable (`resolveSeries` remonte la chaîne `rescheduled_from`) — précaution vaine si l'autre moitié change : l'agenda ne reconnaît plus l'UID, un déplacement DUPLIQUE le rendez-vous et une annulation est IGNORÉE. Cascade : `NEXT_PUBLIC_APP_URL` → `NEXT_PUBLIC_SITE_URL` → **`VERCEL_PROJECT_PRODUCTION_URL`** (l'alias de production, STABLE — `<projet>.vercel.app` suffit, aucun domaine acheté n'est requis) → `VERCEL_URL` (l'URL du DÉPLOIEMENT, NEUVE à chaque mise en ligne : dernier recours, et un piège) → localhost. Une variable définie mais VIDE ne masque plus le repli suivant, et un schéma oublié est complété.
  - **EXEMPTION PROXY — une seule entrée : `/api/sched`** (`src/proxy.ts`, `API_SELF_AUTHENTICATED`). ⚠️ **Les pages `/r/` et `/b/` n'ont besoin d'AUCUNE exemption** : le régime « pages » du proxy est une **liste blanche** (`PROTECTED_PREFIXES`), donc tout ce qui n'y figure pas est déjà public — les y inscrire ferait croire à une garde qui n'existe pas. Seul `/api/*` est deny-by-default, d'où l'entrée unique. S'y ajoutent, pour `/r/ /b/ /api/sched/` uniquement : un **court-circuit d'authentification** (évite un aller-retour Supabase toujours nul sur le chemin critique d'une page ouverte depuis un mail) et les en-têtes **`noindex` / `no-store` / `no-referrer`** (ce dernier parce que le jeton est dans l'URL et partirait sinon dans le `Referer`).
  - **RATE-LIMITING EN BASE** (`sched_rate_limits` + fonction SQL `sched_rate_limit_hit`, fenêtre fixe, incrément atomique). Un compteur en mémoire de process serait **décoratif** sur serverless — c'est la leçon du double mail : entre deux instances, seule la base est partagée. Politique de panne **fail-open** : compteur injoignable ⇒ on laisse passer (la limitation protège d'un abus, elle n'est pas le contrôle d'accès — celui-là c'est le jeton). Purge des fenêtres périmées **rattachée au drain d'événements**, pas de mécanisme dédié.
  - **UID d'agenda = RACINE de la chaîne `rescheduled_from`**, jamais `booking.id` : un déplacement crée une nouvelle ligne, donc utiliser son id ferait apparaître un **second** événement dans l'agenda au lieu de déplacer le premier. `SEQUENCE` = rang dans la chaîne (+1 à l'annulation). Générateur `.ics` **interne au module** (`ics.ts` — la frontière interdit d'importer `src/lib/calendar/`) : `METHOD:REQUEST` avec `ORGANIZER` = `notify_email` du titulaire (les réponses arrivent chez une vraie personne) et `ATTENDEE;RSVP=FALSE`, repli `PUBLISH` sans organisateur, `CANCEL` à l'annulation. Le `.ics` part typé `text/calendar; method=…` — d'où le `contentType` ajouté à `sendEmail` ; sans ce paramètre les clients affichent une pièce jointe ordinaire au lieu d'une invitation.
  - **CLÉ D'IDEMPOTENCE DU LIEN = l'identifiant d'ANALYSE, jamais l'uid brut** (`src/lib/imap/analysis-id.ts`). L'uid IMAP n'est unique que PAR BOÎTE et `campaign_mailboxes` est n:n : deux candidats de deux boîtes peuvent porter l'uid `102` sur la même campagne ⇒ même clé, **même jeton**, prénom de l'autre sur la page, révocation qui frappe le mauvais lien. `context = { uid, analysisId, campaignId }` — l'`uid` retrouve le briefing (`getPendingBriefByUid`, plus jamais l'email en natif), l'`analysisId` identifie la candidature. Validations HITL en vol : repli dérivé de l'identifiant de validation (`src/lib/hitl/analysis-key.ts`, pur/testé). **Réinvitation = NOUVELLE génération de clé** (`analysisId#r2`) — un lien est à usage unique, ré-émettre avec la clé d'origine rendrait fidèlement le jeton mort ; la révocation frappe TOUTES les générations.
  - **Le consommateur d'événements n'est branché QUE sur le rail de drain.** `emitEvent` dispatche EN LIGNE dans la requête de confirmation : brancher le consommateur sur les surfaces candidat ferait attendre le candidat pendant le téléchargement du CV et l'envoi du briefing, avec un `maxDuration` en face. Deux fonctions distinctes (`scheduling-host/configure.ts`) : `ensureSchedulingConfigured()` (ports, partout) vs `ensureSchedulingConsumer()` (drain seul). Sans consommateur, la ligne d'outbox ATTEND — ce n'est pas un échec. **Coût assumé : le briefing part dans la minute.** Drain déclenché par le cron de relève (prod), le tick du scheduler (dev/VPS) et l'ouverture de l'onglet Entretiens (`after()`).
  - **Le flag ne voyage JAMAIS dans un snapshot.** `campaignToRow` n'écrit pas `scheduling_native` et le type `CampaignSnapshot = Omit<ActiveCampaign,'schedulingNative'>` l'interdit **à la compilation** ; seul le PATCH ciblé l'écrit (garde `owner_not_bookable` : référent actif + disponibilités configurées). Sans ça, un onglet ouvert avant l'activation remettrait le flag à `false` à la première sauvegarde. Test dédié sur la ligne réellement envoyée.
  - **Deux verrous d'émission.** (1) **Sonde sans effet** `canInviteForCampaign` pour le gate d'envoi (il s'exécute avant de savoir si le mail partira — émettre là laisserait un jeton orphelin par envoi avorté) ; (2) **un refus ne mint JAMAIS** : la résolution n'est appelée que pour `mode==='invite'`, sinon un modèle de refus contenant `[lien d'agenda]` (le DRH part de son modèle d'acceptation) émettrait un lien dans un mail de refus. **Pas de repli Cal.com en régime natif** : référent sans disponibilités ⇒ invitation BLOQUÉE (`native_link_unavailable`). **Identité visuelle** = configuration d'installation (`configureScheduling({branding})` + `updateSchedulingIdentity` pour un rafraîchissement PARTIEL — reposer les ports emporterait le transport de l'hôte, défaut attrapé par S14) ; nom d'organisation **non dupliqué** (`interviewConfig.organisationName` canonique, cascade `resolveOrganizationName`). **Référent par défaut = créateur** à la création ; `null` explicite reste « aucun référent ».
  - **Onglet « Entretiens » = poste de pilotage du cycle, adossé aux marquages EXISTANTS.** Source unique `interview_briefs` (`awaiting_booking` / `scheduled`) — donc **les deux régimes par construction**, aucun filtre par régime : un briefing Cal.com et un briefing natif ne diffèrent que par l'origine de leur `booking_uid`. Deux onglets : « En attente de réservation » (ancienneté depuis `updated_at` — un dossier REMIS en attente compte depuis sa remise, sinon il naît « en retard de trois semaines » ; état du lien natif joint par `uid → analysisId`, une requête pour toute la page ; `null` en Cal.com et c'est écrit, pas laissé vide) et « Programmés » (les passés **non pointés en tête**, puis à venir, puis en attente de verdict). Actions = `markCandidateInterview` / `markCandidateValidation` / `dismissCandidature` — **aucun état parallèle**, `deriveCandidateStage` propage seul au ruban, au Bureau et aux rapports. Ne montre QUE des candidatures encore ouvertes (`stageFor` ∈ {invite, rdv_pris, entretien_fait}) : un briefing survit à une décision prise ailleurs, et proposer de pointer un entretien déjà tranché serait rouvrir un dossier clos. Compteurs d'onglet = `count: exact` + keyset (jamais la longueur d'une liste).
  - **NO-SHOW : le dialog AVANT le marqueur.** `candidate_interview_marked{missed}` dérive vers `non_retenu` — c'est donc une DÉCISION, pas un constat. Le dialog s'ouvre d'abord (« Classer non retenu » / « Re-proposer un créneau ») et le marqueur n'est posé que sur le premier choix ; le second ne pose RIEN et se contente de réémettre (trace `interview_link_reissued`). ⚠️ Ne PAS introduire une 3ᵉ valeur de marqueur pour dissocier le fait de la décision : ce serait l'état parallèle que tout le lot évite.
  - **Signal 3 « RDV passé sans action »** (`interviews_awaiting_pointing`, registre `business-signals`) : briefs `scheduled` dont `interview_end_at` < now − `interviewPointingAgeHours` (24 h, seuil centralisé) et dont l'étape est encore `invite`/`rdv_pris` — `entretien_fait` relève du signal 2, on ne réclame pas deux fois la même chose. **Extinction par construction** au marquage. `BusinessSignalTarget` étendu avec `{tab:'entretiens', section}`. Un rendez-vous passé n'est JAMAIS auto-transitionné : le système signale, l'humain acte.
  - **Pièges vérifiés par tests, à ne pas réintroduire** : reporter le `manage_token` avant d'annuler l'ancienne réservation viole l'index unique et remonte un faux `slot_taken` (report en 3 temps) ; traduire toute violation 23505 en `slot_taken` masque le vrai défaut (`isSlotClaimConflict`) ; Postgres rend `+00:00` là où le moteur produit `.000Z` (normalisation ISO canonique dans `rows.ts`) ; `slot_taken` n'existe que sous concurrence RÉELLE — une demande séquentielle sur un créneau pris rend `invalid_slot` (les deux se traitent pareil côté écran). Règles hebdo en **minutes locales** + `weekday` **ISO 1-7** (aligné Luxon). Régression **S13** (cœur) et **S14** (surfaces) ; harnais de démonstration hors ORQA : `npm run demo:scheduling [-- --keep]`.

- **Relève IMAP : bord récent, budgets bornés, aucun saut muet (20-21/08/2026).** Trois défauts empilés faisaient qu'une boîte fraîchement branchée n'était **jamais relevée**, sans une ligne de trace (`last_polled_at`/`last_uid_seen`/`last_error` tous `null`). (1) **Une boîte neuve repartait de l'uid 1** — `buildFetchSet(null)` rendait `'1:*'` avec la source COMPLÈTE : sur 25 688 messages et 50 par poll, ~514 relèves avant d'atteindre le courrier du jour, et la boîte lente cadençait tout le poller. `initialCursorFor` (pur, testé) pose désormais le curseur au **bord récent** : juste avant le plus ancien message reçu **depuis la date de création de la boîte dans ORQA** (cas nominal de recette : on branche, on s'envoie un CV), à défaut juste avant `uidNext`, `null` si indéterminable (l'appelant garde son repli, on n'invente pas). Persisté HORS connexion + journal `imap_mailbox_baseline_set` qui DIT que les messages antérieurs ne seront pas analysés. Le passé se rejoue à la demande, il ne bloque pas le présent. (2) **`client.logout()` sans borne rendait la boîte MUETTE** : après un `break` sur un fetch large le serveur streame encore, `socketTimeout` ne couvre pas ce cas (le socket reçoit des données), mesuré **toujours bloqué à 240 s** — or TOUTES les écritures d'état viennent après ce point. `withTimeout` + `closeConnection` : logout poli borné à 10 s puis coupure sèche, ne lève jamais. (3) **`maxDuration = 60 s` sur Vercel** : l'ouverture seule d'une boîte lente coûtait ~62 s, l'invocation était tuée avant toute écriture. Budget **total de 20 s partagé** entre connexion et SELECT (`MAILBOX_OPEN_BUDGET_MS`), au-delà `last_error = open_timeout` + journal. **Dossier relevé configurable** (`mailboxes.folder`, NULL ⇒ INBOX — `getMailboxLock` n'est plus en dur) : brancher une messagerie PERSONNELLE faisait défiler tout le courrier privé devant l'analyse (107 pièces jointes sans rapport accumulées dans `imap_unmatched_cvs`). ⚠️ **Ce n'est PAS un correctif de latence** — mesuré en isolant la variable, le coût d'un SELECT est **indépendant de la taille du dossier** (~10 s sur un compte, y compris sur un dossier VIDE, contre 0,2 s sur un autre) : la lenteur est propre au COMPTE (limitation du fournisseur), pas à la boîte. **Aucun saut muet** : action `imap_mailbox_skipped { mailboxId, label, reason }` sur `already_in_flight`, `no_campaign_associated` (qui écrivait `last_error: null` — l'écran affichait « relevée, aucune erreur » alors que rien n'avait été fait), `open_timeout`, `select_failed`. Critère de SÉLECTION des boîtes, pour mémoire : `is_enabled = true` et **rien d'autre** (aucune jointure campagne). Cf. mémoire `project_imap_poll_budget`.

- **Jours fériés = une PROPOSITION exhaustive, jamais un filtre (21/08/2026).** Le moteur de créneaux ignore le calendrier civil : un 11 novembre est proposable comme n'importe quel mercredi. `src/lib/calendar/french-holidays.ts` (PUR — comput grégorien de Pâques, 11 fériés métropolitains, aucune table à maintenir) vit **côté hôte**, jamais dans `src/lib/scheduling/**` : un calendrier national y serait la règle d'un seul pays gravée dans un composant réutilisable. L'hôte calcule et pose des **exceptions ordinaires** via l'API existante. Le bouton « Ajouter les jours fériés » est **EXHAUSTIF** : une première version filtrait sur les jours travaillés du recruteur et escamotait 6 fériés sur 14 (dont Noël, un vendredi) **sans rien dire** — la grille CHANGE, et une ligne en trop se voit alors qu'une ligne manquante, non. Le filtre par jours ouvrés appartient au **signal** (« un créneau est-il réellement offert ? »), pas au bouton (qui déclare le calendrier). **Signal 4** `availability_holidays_unblocked` : borné sur l'HORIZON de la ressource, jour courant pris dans SON fuseau, éteint par construction dès qu'une absence est posée. Premier signal porté par un `SignalContext { recruiterId }` — l'agenda est un réglage PERSONNEL, les trois autres signaux restent communs ; un admin ne voit que le sien. Nouveau `BusinessSignalTarget` `{ route }` (les Paramètres ne sont pas un onglet). Hors périmètre assumé : Alsace-Moselle et outre-mer.

- **Un écran terminal ne propose pas de défaire, et sait se refermer (20/08/2026).** L'écran qui suit une réservation ne propose plus NI déplacement NI annulation : ces gestes vivent dans le message de confirmation, qui portait déjà le lien — le besoin naît des jours plus tard, devant la boîte de réception. Il DIT où c'est passé (`manageInMail`) : retirer une option sans dire où elle est ne la déplace pas, ça la supprime. L'écran « vous avez déjà réservé » GARDE le lien (on y arrive en rouvrant délibérément son lien). Tout écran terminal offre une **sortie** (`CloseButton`) : ⚠️ `window.close()` ne ferme QUE ce qu'un script a ouvert — la fermeture est TENTÉE puis le bouton cède la place à un repli (« vous pouvez fermer cet onglet »), un bouton qui échoue en silence ferait douter du reste de l'écran.

- **Jobboard de démonstration : la démo emprunte le chemin de PRODUCTION (21/08/2026).** Plateforme d'emploi FICTIVE (« TalentBoard », `/jobs` + `/jobs/[id]`) pour dérouler en rendez-vous la boucle annonce → publication → candidature → analyse. Spec : **`docs/specs/demo-jobboard.md`** ; script commercial : `docs/ops/script-demo-jobboard.md`. **Le formulaire N'INJECTE RIEN** : `POST /api/jobs/apply` compose un vrai mail (objet `Candidature — <titre> (CAMP-YYYY-NNN)`, CV en PJ avec `contentType`) et l'ENVOIE via `sendEmail` à la boîte associée à la campagne ; la relève IMAP fait le reste. Une démonstration qui court-circuite le transport démontre le pipeline, pas le produit — le prix est **une minute d'attente**, assumée et scénarisée. L'analyse du point d'injection direct (`processEmailAttachment`, mailbox synthétique `is_enabled=false`, et pourquoi PAS `/api/cv-analyzer` — fiche+seuils venant du CLIENT, aval du chat orchestré côté navigateur par `dispatchPostAnalysisOutreach`, idempotence `(mailbox_id, uid)` absente) est CONSERVÉE en §6 de la spec pour un éventuel mode instantané. **Canal « Annonce générique »** : il existait déjà dans l'énum sans contenu ; il en a un, DANS la section « Canaux de diffusion » existante. **Rédiger ≠ publier** — deux routes distinctes : `…/job-post/generate` ne persiste RIEN, `PUT …/job-post` fige le texte tel qu'il est à l'écran (principe du preview HITL). Le contenu vit dans `demo_job_posts` et **jamais dans le snapshot campagne** (`campaignToRow` ne le connaît pas — piège `scheduling_native` : un onglet ouvert avant la publication l'effacerait au premier autosave) ; localisation/contrat figés au snapshot. **La référence EST `CAMP-YYYY-NNN`**, la même chaîne sur l'annonce publique, dans l'objet du mail et dans ORQA — un `REF-…` cosmétique casserait le fil de traçabilité qui fait la démo. Gardes : **flag `DEMO_JOBBOARD_ENABLED=1` fail-closed strict** (absente/vide/`0`/`true` ⇒ 404 sur les 2 pages ET les 4 verbes d'API ; 404 et non 403 — un 403 confirmerait la surface ; PAS un `NEXT_PUBLIC_*`, le panneau ORQA se retire sur le 404) ; proxy = **une seule entrée** `/api/jobs/apply` dans `API_SELF_AUTHENTICATED` (les pages n'ont besoin d'AUCUNE exemption — liste blanche — elles y sont pour `noindex`/`no-store`) ; **débit en base FAIL-CLOSED** (3/10 min/IP, RPC `sched_rate_limit_hit` réutilisée, consommé AVANT le multipart) — ⚠️ **politique INVERSÉE vs réservation** : là-bas fail-open car le jeton est le contrôle d'accès ; ici aucun jeton, la limite est la seule borne et chaque requête envoie un VRAI mail ; upload 10 Mo (< 15 Mo de cv-analyzer : un fournisseur d'envoi est plus strict), PDF/DOCX par **MIME OU extension** (même porte que le poller) ; **pas de jeton d'idempotence** ⇒ bouton désarmé dès le clic (deux clics = deux mails = deux candidatures) ; l'offre doit être **visible** (sinon une campagne dépubliée resterait ouverte aux dépôts). Aucun faux succès : pas de boîte ⇒ 503 tracé, transport KO ⇒ 502 tracé. Journal `demo_jobboard_application_sent` avec `campaignStatus`/`scoringSheetValidated` **au moment du dépôt** (un « CV jamais arrivé » se diagnostique sur pièces). ⚠️ L'adresse du candidat est extraite **du CV**, jamais du formulaire ni de l'expéditeur ⇒ CV de démo en **sous-adressage** de la boîte de démo, jamais une adresse tierce ni un `@demo.local` qui rebondirait. Nettoyage `npm run reset:demo-jobboard` = **annonces seulement** : une candidature du jobboard est un mail ordinaire, rien ne la distingue en base et l'effacer demanderait de deviner.

- **Une fenêtre se pose sur ce qu'on VEUT, jamais sur le flux brut (21/08/2026).** Le fil d'activité du Bureau s'est vidé en prod. Cause : `/api/metrics/global` chargeait les **500 dernières lignes BRUTES** du journal (`METRICS_WINDOW`) puis `journalToActivityFeed` jetait celles qu'il ne savait pas rendre (`default: return null`). Or `imap_mailbox_skipped` (ajoutée le 20/08 pour l'observabilité) était réécrite **à CHAQUE relève** : sur une boîte en `open_timeout` permanent + cron à la minute = **1 440 lignes/jour**. Mesuré en dev : **475/500 lignes**, le fil demandait 50 items et en obtenait **11**, et **37 des 50** derniers évènements affichables étaient **hors fenêtre**. ⚠️ Le piège de raisonnement à ne pas refaire : « au pire on n'ajoute pas de nouveaux items » est FAUX — `useDashboardData` fait `setData(json)` (**remplacement intégral toutes les 5 s**, aucune accumulation), et la fenêtre **glisse** : chaque ligne de bruit **ÉVINCE** une ligne ancienne. Ce n'est pas un seau qu'on remplit, c'est une lampe torche sur les 500 dernières lignes. **Correctif structurel** : `listRecentJournalEntriesByActions(actions, limit)` + `fetchRecentRowsForActions` — la limite porte sur les lignes VOULUES (`.in('action', …)` en SQL). Le fil demande « les 50 derniers évènements AFFICHABLES » (marge ×3 pour les lignes écartées sur leur CONTENU : envoi non abouti, `mailSent:false` — part bornée par la nature des lignes, pas par le volume) ; les métriques agents idem sur `AGENT_METRIC_ACTIONS`. Repli sur la fenêtre brute si le fetch ciblé échoue. Mesuré après : **22 → 50 items**, portée remontée de 8 h à 3 jours. **Anti-divergence OBLIGATOIRE** : `ACTIVITY_FEED_ACTIONS = Object.keys(ACTIVITY_RENDERERS)` et `AGENT_METRIC_ACTIONS = Object.keys(ACTION_TO_AGENT)` — le `switch` a été converti en **registre** exprès ; une liste maintenue à côté finirait par diverger, et la divergence serait **silencieuse** (action chargée mais jamais rendue, ou rendue mais jamais chargée). Test dédié qui vérifie les DEUX sens. **Correctif à la source** : `traceMailboxSkipped` ne journalise plus que les **TRANSITIONS** (première occurrence ou changement de cause), via `mailboxes.last_skip_reason` (migration) — mémoire en BASE, jamais en process (invocations isolées) ; effacée par un poll abouti (écriture **séparée + conditionnelle + best-effort**, pour que le code soit déployable AVANT la migration et qu'un échec n'emporte pas l'écriture d'état du poll). L'intention du 20/08 est intacte : un opérateur voit toujours qu'une boîte est sautée et pourquoi (`repeatedUntilChange: true`, `previousReason`). **Signaux métier du fil enrichis** : RDV pris/déplacé/annulé, entretien réalisé, entretien à programmer, nouveau créneau proposé, décision HITL envoyée / **décidée sans envoi**, dossier en attente de validation, classement sans suite (**ton neutre, jamais « refus »**), rapport envoyé, candidature jobboard. ⚠️ `interview_brief_delivered` ne portait que `attendeeEmail` : `attendeeName` + `startAt` ajoutés aux DEUX écrivains (webhook Cal.com + consommateur natif), couplage gardé par test — un fil qui parle en identifiants ne se lit pas. Restent HORS du fil les évènements vivier (`vivier_invitation_sent`, `vivier_application_matched`) : leur payload ne porte qu'un `candidateId`, et « invitation envoyée à 7f3a-… » est pire que le silence.

- **« Un non sans preuve n'est pas un verdict » — le pré-filtre ne juge pas (21/08/2026).** Incident CAMP-2026-288 : un consultant AMOA / Trade Finance scoré **0/100, 4 critères sur 4 à « non »**, sans qu'aucun modèle n'ouvre son CV. Ni l'extraction (3 197 caractères propres) ni le prompt : le pré-filtre par mots-clés, conçu comme accélérateur, **jugeait** — mot-clé absent ⇒ « non ». Le CV disait « Consultant SI & AMOA » quand la fiche cherchait « Consultant MOA ». **RÈGLE** : un mot-clé **absent ne prouve rien** ⇒ le critère part au modèle, pour TOUTE méthode. Ce que fait un mot-clé **trouvé** reste asymétrique et c'est délibéré : `keywords_exact`/`keywords_with_variants` concluent en local (économie d'appel gardée) ; **`hybrid_keywords_llm` va TOUJOURS au modèle** même sur gardien trouvé — « MOA » peut apparaître dans « j'ai assisté le Consultant MOA », candidat OBJET et non sujet ; conclure sur la présence y fabriquerait des faux POSITIFS symétriques. Un critère déféré arrive **NU** devant le modèle (sans la liste qui vient d'échouer — ne pas l'ancrer sur un vocabulaire inadapté). **INVARIANT testé** (`src/lib/scoring/verdict-integrity.ts`, garde `assertNoUnprovenNegative` qui LÈVE) : aucune décision négative ne sort d'un chemin qui n'a pas lu le CV. ⚠️ L'invariant est « aucun négatif sans **lecture** », PAS « sans citation » : un « non » du modèle sur une absence réelle n'a aucune citation à donner, et l'exiger le pousserait à en inventer une ; `non_verifiable` reste le marqueur honnête de « non évalué ». **`decidedBy`** (`keyword_match`|`llm`) porte le chemin RÉELLEMENT emprunté — `verificationMethodUsed` ne dit que la méthode DÉCLARÉE, et cette confusion est ce qui a rendu l'incident illisible. **Réparation du parc** : `npm run rescore` (`scripts/rescore-analyses.ts`), chemin DÉDIÉ — le rejeu C11 ne convient pas (id d'analyse insert-only, claim posé : un rejeu naïf ne ferait rien ou **renverrait un mail**). Trois garanties : (1) **aucun envoi, jamais**, même en `auto_accept` (une invitation deux semaines après coup, sur une campagne peut-être pourvue, fait plus de mal qu'un refus) ; (2) garde **en SQL** (`decided_by='auto'` + `dismissed_at is null`) — un script lancé à la main ne la contourne pas ; (3) sélection **auto-limitante** (signature d'époque + `decidedBy !== 'llm'` ⇒ un dossier réparé sort du périmètre). 4ᵉ garde : zone **`auto_reject` = `reject_sent`**, refus RÉELLEMENT parti ⇒ simulé, jamais appliqué en silence. ⚠️ Le re-scoring rejoue le dossier **ENTIER** (le modèle n'est pas déterministe) : lire +20 comme un signal, ±10 comme du bruit. Bilan et arbitrages ouverts : **`docs/ops/reparation-scoring-2026-08-21.md`**.

- **Corriger une décision = POSER un nouveau marqueur, jamais en effacer un (25/08/2026).** L'étape n'est stockée nulle part : elle est DÉRIVÉE (`deriveCandidateStage`) de marqueurs journalisés en **ajout seul, dernier-gagne**. Une décision posée par erreur se répare donc en posant l'état voulu — jamais en supprimant ni en réécrivant une entrée. Action « Corriger la décision » sur la fiche candidature (panneau ET page) et dans Entretiens (section « en attente de verdict ») ; **aucune garde de rôle** (tout est tracé). **Dialog OBLIGATOIRE** (jamais d'action en un clic), dans cet ordre : état actuel → **⚠️ ce qui a déjà été déclenché** → nouvel état → motif libre. Le bloc d'effets n'est **JAMAIS vide** : quand rien n'est parti on l'écrit (un bloc absent se lit « pas vérifié »). **Trois invariants**, par ordre d'importance : (1) **aucun envoi, jamais** — même quand le nouvel état correspondrait normalement à un envoi (invitation, refus) ; garde **structurelle** testée : `decision-correction.ts` n'importe aucun émetteur ; (2) **aucun chemin d'écriture parallèle** — writers canoniques uniquement (entrée bâtie par `decision-markers`, `updateCandidateAnalysisDecision`, `reopenCandidature`) ; (3) **cible validée côté serveur** contre les options RELUES (409 `invalid_target`) — le client ne décide pas de ce qui est corrigible. **La gomme `cleared`** (3ᵉ valeur des deux marqueurs) est la pièce maîtresse : « j'ai cliqué sur la mauvaise ligne » ne se répare PAS par une bascule (passer « réalisé » → « absent » poserait une DÉCISION, l'absence dérivant vers `non_retenu`). Ce n'est pas la 3ᵉ valeur que le module d'entretien s'interdit (celle-là dissociait le fait de la décision) : `cleared` ne dérive vers AUCUNE étape. ⚠️ **Piège des 5 lecteurs** : un filtre sur la VALEUR placé AVANT la comparaison de DATES fait gagner le marqueur ANTÉRIEUR — la correction est alors silencieusement ignorée. D'où `foldInterviewMark`/`foldValidationMark` (`src/lib/candidatures/decision-markers.ts`, **seul module autorisé à lire `payload.status`**) : `switch` exhaustif + `assertNever` ⇒ une valeur ajoutée sans traitement **ne compile pas**. Lecteurs recâblés dans le même commit : `stage-signals`, `derive-metrics` (pliage + les 2 renderers du fil — l'ancien `else` affichait « Entretien non réalisé » sur une gomme), `timeline-facts`, `metrics` ; un payload illisible rend un item NEUTRE, jamais `null` (une action chargée et jamais rendue est le défaut du 21/08). **Familles hors marqueur, non forcées dans le moule** : décision HITL / refus auto = **colonnes** (`updateCandidateAnalysisDecision`, `decided_by='user'`) — la **remise en attente de validation n'est PAS offerte** (ressusciter une file n'est pas une correction, et c'est dit dans le dialog) ; classement sans suite = **colonnes + satellites** via `reopenCandidature` (le « Rouvrir » perd son `window.confirm` au profit du dialog partagé) ; `refus_auto` LEGACY est **requalifiable** (le dialog dit franchement que le mail est réellement parti). **Seule exception au « on ne touche à rien d'autre »** : un lien de réservation encore ACTIF est révoqué quand la correction ÉCARTE — c'est ce que fait déjà le refus normal, une révocation ne notifie personne, et un candidat écarté qui garde un lien vivant peut réserver sur un dossier fermé ; ANNONCÉ avant confirmation et tracé (`linkRevoked`). Régime Cal.com : « aucun objet lien à interroger » est **dit**, jamais laissé vide. **Auteur** : `/api/journal` complète désormais `actorEmail`/`actorUserId` depuis `getApiUser()` **côté serveur** (additif, non destructif, best-effort) — les marqueurs historiques n'en ont aucun et le dialog affiche « auteur non enregistré » plutôt qu'un nom inventé. `decision_corrected` alimente la frise (rang 10 — le fait corrigé RESTE, la correction se pose après) et le fil d'activité. Périmètre GELÉ : pas d'undo universel, pas d'historique de versions, pas de correction en masse. Régression **S16**.

## Règles absolues

- **`scripts/migrate.sql` = fichier d'ÉTAT FINAL idempotent**, rejoué EN ENTIER à chaque application (dev et prod). Toute modification de schéma doit : (1) être **rejouable sur n'importe quel état de base** sans erreur (`if not exists`, `drop constraint if exists` + add, `create or replace`, guards sur la DÉFINITION finale — pas seulement sur le nom) ; (2) ne **JAMAIS empiler deux versions d'une même contrainte/structure** — si un chantier fait évoluer une contrainte existante, il **met à jour le bloc d'origine** dans le fichier (un bloc canonique par contrainte) ; (3) être **validée par DOUBLE application** : le fichier doit passer deux exécutions successives en dev sans erreur avant tout déploiement. Incident de référence (30/07/2026) : le CHECK `pending_validations_status` posé à 3 valeurs par un ancien bloc alors qu'un bloc ultérieur (chantier sans-suite) l'avait porté à 4 et que des rows `'void'` existaient — violation 23514 au rejeu. C'est cette classe d'erreur que la règle élimine.
- **TypeScript strict, jamais de `any`.** Si un type est complexe, on le définit dans `src/types/`.
- **Chaque agent implémente le contrat `AgentContract`** défini dans `src/types/agent.ts`. La prise d'acte des actions UI fait partie du contrat du Manager (voir spec §4.1).
- **Chaque composant React = un fichier, max 200 lignes.** Au-delà, on découpe.
- **Tous les appels AI passent par `src/lib/ai/provider.ts`**, jamais directement à OpenAI. Cela centralise les retries, le logging, et la mesure des coûts.
- **Les clés API sont dans `.env.local`**, jamais hardcodées, jamais commitées.
- **Tests vitest avant commit.** Le projet a déjà 42 tests verts en Session 1 — on ne régresse pas.
- **`npm run typecheck` avant chaque commit important.** Le typecheck du projet a `incremental: true` : un `.tsbuildinfo` périmé peut produire des **faux négatifs** (erreurs de type masquées par le cache). `npm run typecheck` force `tsc --noEmit --incremental false` et donne l'état réel. Le typecheck incrémental (`tsc --noEmit`) reste pour le dev local en boucle rapide.
- **Commits conventionnels** : `feat:`, `fix:`, `refactor:`, `test:`, `docs:`, `chore:`.

## Architecture agents

- Chaque agent = `{ id, name, role, skills[], inputs[], outputs[], trigger, humanValidation, individualToggle }`
- Le **Manager RH** est le point de contact conversationnel du donneur d'ordre (SPOC), **en LECTURE SEULE** : il consulte, analyse un CV, fait le point sur une campagne, et **oriente** vers l'UI où l'humain agit. Il ne mute jamais l'état et n'orchestre plus de cadrage write (cf. décision « Manager RH = agent LECTURE SEULE » ci-dessus). **Toute écriture** (création/édition/activation de campagne, statuts, scoring, flux, présélections) se fait **exclusivement via l'UI déterministe** (onglet Campagnes, formulaires, boutons), jamais par le chat. Les agents exécutants (Job Writer, Publisher, CV Analyzer, Scheduler, Rejection Writer) restent déclenchés par l'UI / les pipelines (upload CV, poller IMAP), pas par une orchestration conversationnelle du Manager.
- Les agents communiquent via **appels de fonction internes** dans le MVP. Les hooks Zustand exposent l'état nécessaire à chaque agent.
- Chaque exécution d'agent produit des **métriques** (durée, tokens, coût, statut, queue depth) — le contrat `AgentMetrics` est défini dans `src/types/agent.ts`.
- Chaque agent expose un **toggle d'activation individuel** et un **toggle de validation humaine indépendant** — voir spec §6.3 (actions directes UI).

## Pré-recherche du Manager — contrat à respecter dès la Session 3

> **⚠️ SUPERSÉDÉ par la refonte « Manager RH = agent LECTURE SEULE ».** Le Manager conversationnel ne **collecte plus** de champs et ne fait plus de pré-recherche — la création de campagne (avec sa pré-recherche d'archives comparables) vit désormais dans l'UI (`CampaignCreateSheet`). `searchExistingJobDescriptions` reste utilisé par le **formulaire**, pas par le chat. Section conservée pour l'historique du contrat d'interface.

Le Manager doit **toujours** appeler `searchExistingJobDescriptions(query)` avant de lancer la collecte des champs manquants, même quand le storage est vide. Cela cristallise le contrat d'interface dès maintenant pour que l'implémentation réelle (Session 5) ne nécessite aucun changement de code dans le Manager.

```typescript
// src/lib/storage/job-descriptions.ts (MVP Session 3)
export async function searchExistingJobDescriptions(query: string): Promise<JobDescription[]> {
  // MVP : retourne toujours [] tant que le storage n'est pas implémenté
  // Session 5 : interroge Supabase + Drive
  return [];
}
```

Trois niveaux progressifs (L1 récupération, L2 suggestion, L3 inspiration) — voir spec §4.1.

## Conventions de nommage

- **Composants React** : PascalCase (`AgentCard.tsx`, `ManagerChat.tsx`)
- **Fonctions/variables** : camelCase
- **Types/Interfaces** : PascalCase préfixé (`AgentContract`, `TaskInput`, `CampaignBrief`)
- **Fichiers utilitaires** : kebab-case (`ai-provider.ts`, `job-descriptions.ts`)
- **Identifiants métier** : `CAMP-XXXX` pour les campagnes, `TASK-XXXX` pour les sollicitations hors campagne (voir spec §3.2), `SITE-XXXX` pour les sites (`SITE-DEFAULT` = site par défaut), `DO-XXXX` pour les donneurs d'ordre

## Modèle de données — donneur d'ordre & site (préparation reporting)

Deux dimensions métier rattachées à une campagne, introduites comme pré-requis du module Reporting (source de vérité : `docs/specs/reporting.md` §2). Toute session future doit en tenir compte.

- **Donneur d'ordre** : la personne (interne à l'organisation **cliente**) qui a **initié** une campagne. **À ne pas confondre avec l'utilisateur ORQA** qui manipule l'interface. Une campagne a **au plus un** donneur d'ordre. Champs : nom (obligatoire), prénom, email pro, rôle/fonction (texte libre).
- **Site** : l'implantation géographique/organisationnelle de rattachement d'une campagne (orgs multi-sites). Une campagne a **au plus un** site. Un site **par défaut** (`SITE-DEFAULT`, seedé par `scripts/migrate.sql`) sert les organisations mono-site. Champs : nom (obligatoire), type/catégorie, ville, code postal.

Implémentation : types `src/types/organisation.ts` ; rows + colonnes `campaigns.site_id` / `campaigns.donneur_ordre_id` (**nullable**, `on delete set null`) dans `src/lib/db/types.ts` + `scripts/migrate.sql` ; repos `src/lib/db/repos/{sites,donneurs-ordre}.ts` ; API `src/app/api/{sites,donneurs-ordre}` ; admin légère (CRUD + archivage soft) dans `/settings` (`SitesManager`, `DonneursOrdreManager`). Les deux liens campagne sont **nullable** (migration douce : vides pour les campagnes historiques, à remplir via l'admin ou — phase ultérieure — la capture conversationnelle au brief Temps 1).

**Rapport de campagne (Reporting Phase 2, livré).** Sous-onglet `Rapport de campagne` du `ReportingHub` (onglet interne, pas de route Next dédiée). Colonnes `campaigns.launched_at` / `closed_at` (**nullable**, posées par `patchCampaign` sur transition de statut ; repli `created_at` / `updated_at`). Logique **pure** dans `src/lib/reporting/campaign-report*.ts` (calcul + tri/filtre + libellés), template PDF `campaign-report-pdf.tsx` sur charte partagée `pdf-theme.ts`. **Cache PDF stable** en Supabase Storage (`uploadArtifactBinary`/`downloadArtifact`), traçabilité génération/envoi via le **journal** (`campaign_report_generated` / `campaign_report_sent`). Proxies documentés (canal = réception, time-to-hire = lancement→clôture, recos par règles) — cf. `docs/specs/reporting.md` §3.7.

**Rapport multi-campagnes (Reporting Phase 3, livré).** Sous-onglet `Rapport multi-campagnes` (onglet interne). **Période libre** (défaut « Ce mois »), filtres recherche/donneur/site, **aperçu réactif client-side** (fetch unique de `/reporting/campaigns`). **Génération à la volée, sans cache** (`Cache-Control: no-store`, date+heure en page 1). Primitives d'agrégation **mutualisées** dans `src/lib/reporting/aggregations.ts` (re-exportées par `campaign-report.ts` pour stabilité), chargement clôturées mutualisé `closed-campaigns-loader.ts`, projection `analysis-datum.ts`. Logique pure `multi-campaign-report.ts`, template `multi-campaign-report-pdf.tsx`, routes `/api/reporting/multi-campaigns/{report,send}`. `SearchableSelect` générique (base de `DonneurOrdreSelect` + `SiteSelect`). Traçabilité envoi = journal `multi_campaign_report_sent` (Option A, pas d'UI). Seuils des recos transverses + limitations (marque employeur ≈ taux de réponse) documentés — cf. `docs/specs/reporting.md` §4.8.

## Le Vivier de candidats (V1–V3 + refonte titre, livré)

Stock interne de CV indexés, réutilisable d'une campagne à l'autre comme **source** de candidats (jamais un canal de diffusion). Source de vérité fonctionnelle : `docs/specs/vivier.md` (§14 = état courant de la présélection ; §3.3/§4.2 décrivent l'ancienne cascade, **explicitement marquées « remplacées par §14 »**). Tout comportement métier s'y règle avant le code.

**Modèle de données (pgvector).** `vivier_candidates` (UUID PK, **dédup par email**, entités structurées en JSONB, `title` + `title_variants text[]` + `title_anchors jsonb` (ancres Bloc 1 : déclaré + 2 derniers postes)), `vivier_embeddings` (`title_embedding vector(1536)` — signal courant ; `embedding` full-CV **nullable, conservé mais plus régénéré**, `provider`/`model` nullable), `vivier_skill_embeddings` (1 ligne = 1 compétence : `skill` + `embedding vector(1536)` + `provider`/`model`, `unique(candidate_id, skill)`) + colonne `vivier_candidates.skills text[]`, `vivier_anchor_embeddings` (1 ligne = 1 ancre : `depth` + `anchor_text` + `embedding vector(1536)`, PK `(candidate_id, depth)` ; RPC `match_vivier_anchors`), `vivier_preselections` (PK `(campaign_id, candidate_id)`, porte `state` + faits datés `contacted_at`/`rejected_at`/`decided_by`/`applied_at` + `match_kind`/`match_term`). Migrations dans `scripts/migrate.sql` (idempotentes, l'utilisateur les applique **manuellement** dans Supabase). RPC : `match_vivier_titles`, `match_vivier_candidates` (legacy), `vivier_pending_by_campaign`.

**Indexation** (`src/lib/vivier/indexing.ts`, asynchrone, idempotente). Un appel LLM extrait **entités + TITRE + compétences + 2 derniers postes** (`entity-extraction.ts`) → upsert entités → variantes **iso-rôle** du titre par bloc (`runTitleVariantsSuggestion`, non bloquant) → `setVivierTitle` → **ancres de titre** (déclaré + 2 derniers postes, variantes par ancre, `setVivierTitleAnchors`) → embedding du **titre seul** → **un embedding par ANCRE** (`replaceAnchorEmbeddings` ; depth 0 réutilise le vecteur du titre) → **compétences atomiques + un embedding par compétence** (`setVivierSkills` + `replaceSkillEmbeddings`), tout non bloquant. On **n'embedde plus le CV entier**. Alimentation à deux portes : upload manuel (`/api/cv-analyzer` en `after()`) et poller IMAP (fire-and-forget), via `feedVivierFromApplication` — **non bloquant de bout en bout**, garde email obligatoire.

**Présélection sur le TITRE + COMPÉTENCES** (`src/lib/vivier/preselection.ts`). Cascade deux blocs (porte d'entrée = TITRE) : **Bloc 1 déterministe MULTI-ANCRES** (`title-anchors.ts` : ancres = {titre déclaré depth 0, dernier poste 1, poste précédent 2}, chacune splittée — `splitTitleIntoBlocks`, le tiret ne sépare QUE entouré d'espaces — + variantes iso-rôle ; match si UNE ancre ∩ {blocs intitulé + variantes poste}, on retient la plus RÉCENTE ; **décote d'ancienneté** `titleAnchorWeights` [1, 0.95, 0.9] ; repli sur le titre déclaré si `title_anchors` vide ; normalisation casse + **accents**) puis **Bloc 2 sémantique MULTI-ANCRES** (`anchor-semantic.ts` : cosinus de l'intitulé ⇄ embedding de CHAQUE ancre via RPC `match_vivier_anchors` ; `pickBestAnchor` : PORTE sur le cosinus BRUT `≥ similarityFloor`, SCORE = brut × décote `titleAnchorWeights` ; repêche un titre déclaré bruité via un poste propre ; repli `match_vivier_titles` sur le déclaré si pas d'ancre-embedding). **Score final = 70% titre + 30% compétences** (`vivierConfig.titleWeight`/`skillWeight`) : les **compétences réordonnent les qualifiés, n'en éliminent aucun**. **Tri global** décroissant, **pas de plafond**, fraîcheur en départage léger, **liste vide = réponse valide**. Variantes du poste via le générateur **iso-rôle** (`title-variants-execute.ts`). Garde-fou d'**espace d'embeddings** : modèle requête ≠ modèle indexé ⇒ échec FORT `embedding_model_mismatch`. Seuils dans `vivierConfig` (Settings, calibration) : `similarityFloor` (entrée titre, départ 0,55 — `npm run vivier:title-distribution`), `skillPerSkillFloor` (couverture par compétence, départ 0,6).

**Compétences set-to-set** (`skill-coverage.ts`, **pure** + testée). UN embedding **par compétence** des deux côtés (jamais moyenné : le barycentre dilue et l'asymétrie N vs M pénalise les spécialistes). Pour chaque compétence **attendue** de la fiche (`key_skills` atomisés, `job-skills.ts`, déterministe, embeddés à la volée), max cosinus sur les compétences du candidat ≥ seuil ⇒ **taux de couverture** + **mapping interprétable** (attente → compétence CV). Côté CV : champ `skills` extrait (hard + soft), stocké en `vivier_candidates.skills` + un vecteur par compétence dans `vivier_skill_embeddings`. V1 : poids égal (criticité = V2).

**Cycle factuel & contact (V3).** `vivier_preselections.state` = `identified → contacted | rejected` (transitions pures `proposal-cycle.ts`, cohérence état↔dates garantie en base par CHECK **et** dans la couche d'accès `markContacted`/`markRejected`). Réconciliation `replacePreselection` **idempotente et non destructive des décisions** (ne ressuscite ni ne supprime jamais un `contacted`/`rejected`). Validation **org-level** `/validations-vivier` (lien + badge `TopBanner`, pas un onglet campagne ; décisions unitaires + en masse, tracées au journal). Invitation à **candidater** (`invitation-template.ts`, déterministe, **mention RGPD systématique** partagée via `rgpd-mention.ts`) : `[référence]` = `CAMP-XXXX`, c'est elle que le poller cherche dans l'objet (`matchCampaignInSubject`) ; l'envoi pose `contacted` (best-effort). **Rapprochement par email** exact aux deux portes (`matchVivierApplication` → `recordApplied`, jamais de fuzzy). **Cooldown** : contacté ⇒ exclusion **globale** (`cooldownDays`) ; rejeté ⇒ exclusion **par campagne** ; sortie automatique à la candidature. Settings `vivierConfig` (jsonb : mode manuel/auto, template, cooldown, plafond, organisation, `similarityFloor`). Métrique de conversion dans le rapport de campagne.

**Scripts.** `npm run reindex:vivier` (régénère titres + variantes iso-rôle + embedding titre + **compétences + embeddings par compétence** ; **obligatoire après tout changement de modèle/représentation** ; avant reindex, skills=∅ ⇒ couverture 0, dégradation douce — `--only-failed`, `--dry-run`), `npm run vivier:title-distribution -- <campaignId>` (calibration du seuil). Changer `OPENAI_EMBEDDING_MODEL` ⇒ reindex **+ redémarrage serveur** (env figé au boot — réflexe `feedback_restart_dev_server`). **Import en masse** d'un stock de CV existant (hors app/Vercel, depuis sa machine) : `npm run import:vivier -- <dossier>` (`scripts/import-vivier.ts`) — réutilise le pipeline d'indexation, statut honnête renforcé (réussi seulement si embedding titre présent), reprise idempotente via journal local, dédup par email (réindexe les dossiers existants non-indexés), backoff 429, confirmation du projet cible par saisie du ref. Procédure complète : `docs/ops/import-vivier-en-masse.md`.

## Ce que Claude Code doit savoir

- **Le projet est un prototype client-facing.** Il doit être visuellement crédible en démo. L'effet wow compte autant que la fonctionnalité — un client doit voir « une équipe » au travail, pas un dashboard technique.
- **La mimétique entreprise réelle est le différenciateur.** Quand un comportement est ambigu, demande-toi : que ferait un responsable RH humain dans cette situation ? Si la réponse est « il ne ferait jamais ça comme ça », c'est que le design est à revoir.
- **Le Manager parle métier, jamais technique.** Pas de « tâche dispatchée », pas de « erreur 401 ». Même quand il orchestre en coulisse, côté donneur d'ordre cela se résume à « je m'en occupe, je reviens vers vous » et « la diffusion sur LinkedIn semble en panne, je publie sur les autres canaux en attendant ».
- **Une seule question à la fois** dans les phases de collecte. Jamais de rafale de questions, jamais de formulaire déguisé.
- **Priorité actuelle** : fonctionnel > beau > performant. C'est un prototype — le code doit être propre et lisible, pas optimisé prématurément.
- **Toujours respecter le périmètre de la session courante** (`SESSION_X.md`). La spec couvre tout, mais une session n'implémente qu'un sous-ensemble. Si une fonctionnalité hors session semble nécessaire, on l'ajoute au backlog plutôt qu'à la session en cours.

## Workflow de session

1. Lire `CLAUDE.md` (ce fichier)
2. Lire `docs/specs/entreprise-virtuelle-rh.md` (spec fonctionnelle)
3. Lire `docs/sessions/SESSION_X.md` (périmètre courant)
4. Implémenter strictement ce qui est dans le périmètre IN
5. Si une question relève du périmètre OUT, le signaler et reporter
6. Tester (vitest), commit conventionnel

## Concurrence et inspiration

**Limova** (concurrent identifié, contrat non signé) propose un SaaS RH classique où tout passe par chat IA. La différenciation QWESTINUM repose sur :

- La **mimétique organisationnelle** (équipe, pas chatbot)
- La **dualité chat / actions directes** (Limova force tout par chat — le donneur d'ordre clique sur ses propres outils)
- L'**option Drive** (le client voit ses livrables apparaître dans son Drive partagé — Limova garde tout en silo)
- Le **positionnement Process First** (cabinet de conseil, pas SaaS — le système s'adapte au processus client, pas l'inverse)

Ces différenciateurs doivent transparaître dans le code et l'UI.
